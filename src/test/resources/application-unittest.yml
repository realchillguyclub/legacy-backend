spring:
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        format_sql: true
  datasource: { }

logging:
  level:
    org.hibernate.SQL: warn
    org.hibernate.orm.jdbc.bind: trace
